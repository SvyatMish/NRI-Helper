import { CustomComponent } from "../../../types";

export const WaterdeepLogo: CustomComponent = ({ className }) => {
  return <img className={`${className}`} src="Waterdeep-symbol.webp" alt="" />;
};
